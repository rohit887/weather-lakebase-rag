command: ["python", "app.py"]

# Database connection is handled automatically by the postgres database resource.
# The app's database resource injects DATABRICKS_POSTGRES_* environment variables
# with host, database, user, port, and password (OAuth token).
# No manual configuration needed here!
