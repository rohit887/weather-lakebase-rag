command: ["python", "app.py"]

env:
  # Lakebase connection - fetches URL from secret directly in code
  - name: LAKEBASE_SECRET_SCOPE
    value: "database"
  - name: LAKEBASE_SECRET_KEY
    value: "lakebase-url"
